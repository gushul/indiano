= simple_form_for @product, :html => {:multipart => true} do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.file_field :photo, label: "Add photo"

  .form-actions
    = f.button :submit
